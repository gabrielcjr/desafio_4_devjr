[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
coverage = "*"
mypy = "*"
black = "*"
flake8 = "*"

[requires]
python_version = "3.10.4"

[scripts]
test = "python -m unittest discover src/test -p *_test.py"
flake8 = "flake8 --ignore E501,F401,E402,W605,W503"
start = "python3 src/main.py"
coverage = "coverage run -m unittest discover src/test -p *_test.py"
coverageReport = "coverage report"
mypy = "mypy --no-warn-no-return"
